# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# Hypridle 
# Original config submitted by https://github.com/SherLock707                                                       
                                                      
$iDIR="$HOME/.config/swaync/images/ja.png"

general {
    lock_cmd = systemctl --user start hyprlock.service # runs robust systemd lock service
    unlock_cmd = systemctl --user stop hyprlock.service # stops hyprlock when unlocking
    before_sleep_cmd = loginctl lock-session    # ensures that the session is locked before going to sleep
    after_sleep_cmd = sleep 1 && hyprctl dispatch dpms on # turn of screen after sleep (not strictly necessary, but just in case)
    ignore_dbus_inhibit = true              # whether to ignore dbus-sent idle-inhibit requests (used by e.g. firefox or steam)
    inhibit_sleep = 3                       # inhibits systemd suspend until hyprlock has locked the screen
}

# turn off screen faster if session is already locked
# (disabled by default)
# listener {
#     timeout = 30                            # 30 seconds
#     on-timeout = pidof hyprlock && hyprctl dispatch dpms off # turns off the screen if hyprlock is active
#     on-resume = pidof hyprlock && hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
# }

# Warn
listener {
    timeout = 150                     # 2.5 min
    on-timeout = notify-send -i $iDIR " You are idle!" # command to run when timeout has passed
    on-resume = notify-send -i $iDIR " Oh! you're Back" " Hello !!!"  # command to run when activity is detected after timeout has fired.
}

# Screenlock
listener {
    timeout = 240                     # 4 min
    on-timeout = loginctl lock-session # command to run when timeout has passed
    #on-resume = notify-send -i $iDIR " System Unlocked!"  # command to run when activity is detected after timeout has fired.
}

# Turn off screen 
listener {
    timeout = 270                            # 4.5 min (offset from screen lock to prevent race condition)
    on-timeout = hyprctl dispatch dpms off  # command to run when timeout has passed
    on-resume = hyprctl dispatch dpms on    # command to run when activity is detected after timeout has fired.
}

# Suspend
listener {
    timeout = 300                            # 5 min
    on-timeout = systemctl suspend # command to run when timeout has passed
    ignore_inhibit = false                   # respect idle inhibitors (e.g. from waybar)
}

