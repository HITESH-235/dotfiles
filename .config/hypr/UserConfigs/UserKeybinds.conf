# /* ---- 💫 https://github.com/JaKooLit 💫 ---- */  #
# This is where you put your own keybinds. Be Mindful to check as well ~/.config/hypr/configs/Keybinds.conf to avoid conflict
# if you think I should replace the Pre-defined Keybinds in ~/.config/hypr/configs/Keybinds.conf , submit an issue or let me know in DC and present me a valid reason as to why, such as conflicting with global shortcuts, etc etc

# See https://wiki.hyprland.org/Configuring/Keywords/ for more settings and variables
# See also Laptops.conf for laptops keybinds 

# /* ---- ✴️ Variables ✴️ ---- */  #
$mainMod = SUPER
$scriptsDir = $HOME/.config/hypr/scripts
$UserScripts = $HOME/.config/hypr/UserScripts
$UserConfigs = $HOME/.config/hypr/UserConfigs

#  IMPORTANT: If you want to remap and existing keybind you MUST unbindd it first 

# The bindings are CASE SENSITIVE. We suggest you copy the exisitng binding here
#  Then change `bindd` to `unbind`

# E.g. 
# unbind = $mainMod, Return, Open terminal, exec, $term
# bindd = $mainMod, Return, Open terminal, exec, ghostty 
#
# unbind = $mainMod, E, file manager, exec, $files
# bindd = $mainMod, T, file manager, exec, $files

# If you are ADDING a bindd, make sure you include the description 
# Other the keybind search menu might not show it properly 

# E.g.









# Clear Cache shortcut
bindd = $mainMod ALT, C, Clear System Cache, exec, $UserScripts/ClearCache.sh

# Fan / Power Profile Cycling
unbind = , xf86Launch4
bindd = , xf86Launch4, Cycle Power Profiles (Fans), exec, $UserScripts/CycleCooling.sh
# bindd = CTRL ALT, F5, Cycle Power Profiles (Fans), exec, $UserScripts/CycleCooling.sh
bindd = $mainMod, F, Cycle Cooling Mode, exec, $UserScripts/CycleCooling.sh

# Custom Screenshot Bindings
unbind = $mainMod, Print
unbind = , Print
unbind = $mainMod SHIFT, Print
unbind = $mainMod CTRL, Print
unbind = $mainMod CTRL SHIFT, Print
unbind = ALT, Print
unbind = $mainMod SHIFT, S

bindd = $mainMod, Print, full screen screenshot, exec, $scriptsDir/ScreenShot.sh --now
bindd = , Print, screenshot region, exec, $scriptsDir/ScreenShot.sh --area
bindd = $mainMod SHIFT, S, screenshot region (swappy), exec, $scriptsDir/ScreenShot.sh --swappy
bindd = $mainMod ALT, S, screenshot full screen (swappy), exec, $scriptsDir/ScreenShot.sh --swappy-now

# Keyboard Backlight (Standard, Alternatives, and Toggles)
binde = , XF86KbdBrightnessDown, exec, $scriptsDir/BrightnessKbd.sh --dec
binde = , XF86KbdBrightnessUp, exec, $scriptsDir/BrightnessKbd.sh --inc
binde = , XF86KbdLightOnOff, exec, $scriptsDir/BrightnessKbd.sh --cycle
binde = , XF86KbdBrightnessToggle, exec, $scriptsDir/BrightnessKbd.sh --cycle
binde = , xf86KbdLightOnOff, exec, $scriptsDir/BrightnessKbd.sh --cycle
binde = , xf86KbdBrightnessToggle, exec, $scriptsDir/BrightnessKbd.sh --cycle

# Overrides for Fn+F4 which default to non-functional asusctl led-mode on this laptop
unbind = , xf86Launch3
binde = , xf86Launch3, exec, $scriptsDir/BrightnessKbd.sh --cycle
binde = , XF86Launch3, exec, $scriptsDir/BrightnessKbd.sh --cycle

# Custom shortcuts (instant, no reboot required):
# Ctrl+Alt + Up/Down for Screen Brightness
# binde = CTRL ALT, Up, exec, $scriptsDir/Brightness.sh --inc
# binde = CTRL ALT, Down, exec, $scriptsDir/Brightness.sh --dec

# Ctrl+Alt + Left/Right for Audio Volume
# binde = CTRL ALT, Right, exec, $scriptsDir/Volume.sh --inc
# binde = CTRL ALT, Left, exec, $scriptsDir/Volume.sh --dec

# Ctrl+Alt + K to cycle Keyboard Backlight
# binde = CTRL ALT, K, exec, $scriptsDir/BrightnessKbd.sh --cycle

# Remap clipboard manager from Super+Alt+V to Super+V
unbind = $mainMod ALT, V
bindd = $mainMod, V, clipboard manager, exec, $scriptsDir/ClipManager.sh

# warpd mouse emulation keybindings
bind = ALT, X, exec, /home/hit235/.local/bin/toggle-warpd.sh

# Unbind default CTRL+ALT shortcuts to prevent IDE conflicts
unbind = CTRL ALT, W
unbind = CTRL ALT, Delete
unbind = CTRL ALT, l
unbind = CTRL ALT, p


# Unbind layout switching to prevent IDE shortcut conflicts
unbind = ALT_L, SHIFT_L
unbind = SHIFT_L, ALT_L

# Monitor Profile Switcher
bindd = $mainMod SHIFT, D, Choose Monitor Profile, exec, $scriptsDir/MonitorProfiles.sh

# Override browser keybind to launch zen-browser cleanly
unbind = $mainMod, B
bindd = $mainMod, B, Open Zen Browser, exec, zen-browser



